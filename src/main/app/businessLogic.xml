<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns:tracking="http://www.mulesoft.org/schema/mule/ee/tracking" xmlns:sfdc="http://www.mulesoft.org/schema/mule/sfdc" xmlns="http://www.mulesoft.org/schema/mule/core" xmlns:doc="http://www.mulesoft.org/schema/mule/documentation" xmlns:spring="http://www.springframework.org/schema/beans" version="EE-3.5.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
xsi:schemaLocation="http://www.mulesoft.org/schema/mule/sfdc http://www.mulesoft.org/schema/mule/sfdc/current/mule-sfdc.xsd
http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-current.xsd
http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
http://www.mulesoft.org/schema/mule/ee/tracking http://www.mulesoft.org/schema/mule/ee/tracking/current/mule-tracking-ee.xsd">
    <flow name="mainFlow" doc:name="mainFlow" doc:description="Here you should place the main business logic.">
        <flow-ref name="gatherDataFlow" doc:name="gatherDataFlow"/> 
        <flow-ref name="filterAndInsertDataFlow" doc:name="filterAndInsertDataFlow"/>
        <logger message="Amount of custom objects #[flowVars['customObjectCount']] - Custom Objects Filtered #[flowVars['filteredCustomObjectsCount']]" level="INFO" doc:name="Logger"/>
        <set-payload value="Amount of custom objects migrated #[flowVars['customObjectCount']] - Amount of custom objects Filtered #[flowVars['filteredCustomObjectsCount']]" doc:name="Set Payload"/>
        <exception-strategy ref="defaultChoiceExceptionStrategy" doc:name="Reference Exception Strategy"/>
    </flow>
    <sub-flow name="gatherDataFlow" doc:name="gatherDataFlow">
        <logger message="Gathering custom object's data" level="INFO" doc:name="Logger"/>
		<sfdc:query config-ref="SalesforceA" query="dsql:SELECT Name, LastModifiedDate, interpreter__c, year__c FROM MusicAlbum__c WHERE year__c &gt; 1968 ORDER BY Name ASC" doc:name="Salesforce" fetchSize="${page.size}"/>
        <set-variable variableName="customObjectCount" value="#[payload.size()]" doc:name="customObjectCount"/>
	</sub-flow>
    <sub-flow name="filterAndInsertDataFlow" doc:name="filterAndInsertDataFlow">
        <logger message="Starting custom objects creation" level="INFO" doc:name="Logger"/>
        <set-variable variableName="filteredCustomObjectsCount" value="#[0]" doc:name="filteredCustomObjectsCount"/>
        <foreach doc:name="For Each" batchSize="${page.size}">
            <flow-ref name="filterFlow" doc:name="filterFlow"/>
            <expression-filter expression="#[!payload.isEmpty()]" doc:name="Filter if Payload is empty"/>
            <sfdc:upsert config-ref="SalesforceB" type="MusicAlbum__c" doc:name="Upsert custom objects in org B" externalIdFieldName="Id">
                <sfdc:objects ref="#[payload]"/>
            </sfdc:upsert>
        </foreach>
    </sub-flow>
    <sub-flow name="filterFlow" doc:name="filterFlow">
        <set-variable variableName="filteredCustomObjectList" value="#[new java.util.ArrayList()]" doc:name="filteredCustomObjectList"/>
        <foreach doc:name="For Each">
            <enricher source="#[payload]" target="#[flowVars['customObjectInB']]" doc:name="Store in variable customObjectInB">
                <sfdc:query-single config-ref="SalesforceB" query="SELECT Id, Name, LastModifiedDate, interpreter__c, genre__c FROM MusicAlbum__c WHERE Name = '#[payload['Name']]'" doc:name="Check custom object in org B"/>
            </enricher>
            <custom-transformer class="org.mule.kicks.transformers.SFDCCustomObjectFilter" doc:name="SFDCCustomObjectFilter"/>
            <expression-filter expression="#[!(payload is NullPayload)]" doc:name="Filter if Payload is NullPayload"/>
            <expression-component doc:name="Add Custom Object to filteredCustomObjectList"><![CDATA[#[flowVars['filteredCustomObjectList'].add(payload)]]]></expression-component>
        </foreach>
        <set-payload value="#[flowVars['filteredCustomObjectList']]" doc:name="Set filteredCustomObjectList as Payload"/>
    </sub-flow>
</mule>
